-- Remote history placeholder.
-- This version exists in the linked Supabase project's migration history from an
-- older migration chain. It is intentionally a no-op so Supabase Preview can
-- validate the protected `main` branch without replaying superseded SQL on new
-- databases.
select 1;
